{
  "previewPort": 3000,
  "healthPath": "/",
  "startCommand": "docker compose -f docker-compose.base44.yml up -d",
  "secrets": [
    {
      "name": "BASE44_APP_ID",
      "description": "The Base44 app id (found in the Base44 editor URL, between /apps/ and /editor/). The app's AI features use Base44's built-in InvokeLLM integration, which bills against your workspace's Base44 integration credits instead of a user-supplied API key. The app UI boots without it; AI endpoints fail until it is set.",
      "requiredAtBoot": false
    }
  ]
}
