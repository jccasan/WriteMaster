# Base44 local defaults — placeholders so the app boots without credentials.
# Real secrets are delivered via /run/base44/app.env (last env_file, always wins).

# Base44 built-in AI integration (InvokeLLM) — bills against the workspace's
# Base44 integration credits. AI features need this app id (from the Base44
# editor URL); the app UI boots fine without it.
BASE44_APP_ID=
