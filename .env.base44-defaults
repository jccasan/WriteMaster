# Base44 local defaults — placeholders so the app boots without credentials.
# Real secrets are delivered via /run/base44/app.env (last env_file, always wins).

# Anthropic / Replit AI integration — only needed for AI-powered features,
# the app UI boots fine without it.
AI_INTEGRATIONS_ANTHROPIC_API_KEY=
AI_INTEGRATIONS_ANTHROPIC_BASE_URL=
